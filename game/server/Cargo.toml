# mochou-p/game/game/server/Cargo.toml

[package]
name          = "game_server"
edition       = "2024"

[dependencies]
database_core = { path      = "../../database/core" }
game_protocol = { path      = "../protocol"         }
tokio         = { workspace = true                  }

